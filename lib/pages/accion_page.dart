import 'package:flutter/cupertino.dart';

class AccionPage extends StatelessWidget {
  const AccionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Accion'),
    );
  }
}
